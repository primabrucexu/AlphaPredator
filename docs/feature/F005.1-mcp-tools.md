# F005.1: MCP Tool 能力接入
> STATUS：设计

## MileStone

| 日期 | 事件 |
|---|---|
| 2026-08-24 | 确认所有业务 Tool 从 F005 基础接入中拆出，由本子需求统一接入 |
| 2026-08-24 | 立项 |

## Relation

- **Blocked by**: F005（先完成无 Tool、Resource、Prompt 的 MCP Server 基础接入和 Hermes 注册验证）
- **Related**: 其他业务 Feature（F005.1 完成后同步更新其 `MCP` 状态）

## What

F005.1 负责选择并实现项目的 MCP Tool。当前只建立子需求边界，尚未选择首批 Tool；其他业务 Feature 在 F005.1 完成前统一保持“待确认”。

## How

- Tool 名称、参数、返回值、权限和错误语义在本 Feature 中讨论确认。
- Tool 必须复用现有 service、`MarketDataProvider` 或 `TaskService`，不得复制业务逻辑。
- F005.1 完成后同步更新来源 Feature 的 `MCP` 状态和 `docs/integrations/mcp.md` 汇总。

## MCP

F005.1 是业务 Tool 的统一接入子需求。其具体能力范围尚未确认，`mcp_status` 暂为“待确认”。

## Acceptance Criteria

- [ ] AC1: 首批 Tool 范围在 F005.1 中确认后才进入实现。
- [ ] AC2: 完成后同步更新来源 Feature 的 `MCP` 状态和 MCP 汇总。
