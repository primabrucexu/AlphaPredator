# 最近操作

> **用途**：记录 agent 最近对所有需求的操作日志，按时间倒序排列。
> **维护规则**：每次会话结束时由 agent 追加；只保留最新 5 条，旧记录自然滚动清理。

| 日期 | 需求 | 动作 |
|------|------|------|
| 2026-06-23 | F02 | 优化 MARKET_DATA 日线导入：按单股查询 DuckDB 最新交易日，首次导入全量，后续从最新日期下一天增量拉取；已存在到任务结束日的股票直接跳过，日线 bulk 写入移除范围 DELETE |
| 2026-06-23 | F02 | 调整数据初始化麦蕊速率/并发语义：页面仅维护每分钟请求速率，后端使用 paced limiter 严格控制总请求速率，并根据实际网络耗时自动升降 MARKET_DATA / MARKET_DATA_5M 并发窗口；相关后端测试通过 |
| 2026-06-22 | F06 | 将 MACD 扫描升级为后台任务类型 `MACD_ALERT_SCAN`，复用 `task_info` 进度体系；前端支持任务进度轮询、当前股票展示和终止任务 |
| 2026-06-22 | 全局规则 | 修复明确违反 SQLite ORM 新规则的生产代码：补齐 SQLite SQLModel 映射，迁移 stock_list、task_info、热点复盘、交易复盘、联动回测和 MACD 预警访问层；目标后端测试通过 |
| 2026-06-22 | 全局规则 | 新增规则：除非经过人工明确批准，SQLite 读写操作默认必须通过项目既有 ORM 框架或 ORM 模型层完成；并按该规则扫描现有 SQLite 访问点 |
