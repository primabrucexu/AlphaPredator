# 最近操作

> **用途**：记录 agent 最近对所有需求的操作日志，按时间倒序排列。
> **维护规则**：每次会话结束时由 agent 追加；只保留最新 5 条，旧记录自然滚动清理。

| 日期 | 需求 | 动作 |
|------|------|------|
| 2026-06-27 | F02 | 将“一键增量更新”改为复用批量初始化接口：新增 `market_mode` 参数，增量按钮传 `INCREMENTAL_SYNC`，同时创建股票列表、行情数据和韭研复盘三类任务；MCP 增量更新工具同步改为批量任务；后端定点测试和前端构建通过 |
| 2026-06-24 | F06 | 将麦蕊日线历史行情接口从不复权 `d/n` 改为前复权 `d/f`；日线写入 DuckDB 时跳过 `is_up_limit` / `is_down_limit` 字段计算并统一保存为 false，避免复权价污染涨跌停口径；相关后端测试通过 |
| 2026-06-24 | F06 | 新增 MACD 个股即时验证能力：后端提供不落库的 `stock-validate` 接口，复用批量预警规则计算截止日触发、最近触发、历史同类样本和回测汇总；前端 MACD 页面增加“个股验证”tab；后端测试、前端构建和 Playwright 冒烟通过 |
| 2026-06-23 | F06 | 优化 MACD 预警历史样本明细展示：主表改为一句话样本说明，点击展开查看买入、卖出、金叉、T+1 状态和形态价格等详细记录；前端构建与 Playwright 冒烟通过 |
| 2026-06-23 | F02 | MCP 服务增加 `start_market_data_incremental_update` 工具：按最近成功 MARKET_DATA 任务的截止日期自动计算增量区间并启动行情任务 |
