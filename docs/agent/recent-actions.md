# 最近操作

> **用途**：记录 agent 最近对所有需求的操作日志，按时间倒序排列。
> **维护规则**：每次会话结束时由 agent 追加；只保留最新 5 条，旧记录自然滚动清理。

| 日期 | 需求 | 动作 |
|------|------|------|
| 2026-06-27 | F06 | 按“交互宽进、内部 6 位代码”规则审计并修复股票输入入口：MACD 个股验证、股票联动回测、交易复盘编辑改为复用股票候选搜索输入；后端股票搜索支持交易所前后缀和中文名称前缀；联动回测接口支持 6 位代码入参并归一到现有 full_code 服务字段；相关后端测试和前端构建通过 |
| 2026-06-27 | F06 | 新增并修正 agent 规则：程序内部股票数据流转统一使用 6 位数字代码；用户和网页交互时对齐首页搜索框，支持股票代码、名称、拼音/首字母、交易所前后缀等多种输入和候选搜索 |
| 2026-06-27 | F06 | 修复 `POST /api/macd-alerts/stock-validate` 常见股票代码输入返回 422：后端 schema 将空白、`SH/SZ/BJ` 前缀和 `.SH/.SZ/.BJ` 后缀规范化为 6 位代码；新增接口回归测试，MACD 接口定点测试通过 |
| 2026-06-27 | F02 | 将“一键增量更新”改为复用批量初始化接口：新增 `market_mode` 参数，增量按钮传 `INCREMENTAL_SYNC`，同时创建股票列表、行情数据和韭研复盘三类任务；MCP 增量更新工具同步改为批量任务；后端定点测试和前端构建通过 |
| 2026-06-24 | F06 | 将麦蕊日线历史行情接口从不复权 `d/n` 改为前复权 `d/f`；日线写入 DuckDB 时跳过 `is_up_limit` / `is_down_limit` 字段计算并统一保存为 false，避免复权价污染涨跌停口径；相关后端测试通过 |
