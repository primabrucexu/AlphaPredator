# 当前执行进度

> **用途**：记录当前活跃需求的进度、下一步和阻塞点。不保留历史需求。
> **维护规则**：每次会话结束时由 agent 更新。

---

## 当前活跃需求
- [F02：市场数据](F02-market-data.md)

---

### 完成情况

- [x] 明确“一键增量更新”需复用“一键全量初始化”的批量接口能力。
- [x] 扩展批量初始化请求，支持 `market_mode`，默认保持 `FULL_SYNC`。
- [x] 一键增量更新调用同一个批量接口，并传入 `INCREMENTAL_SYNC`。
- [x] 一键增量更新现在会创建三类任务：`STOCK_LIST_SYNC`、`MARKET_DATA`、`JYGS_REVIEW`。
- [x] MCP 增量更新工具同步改为创建增量批量任务。
- [x] 后端定点测试通过。
- [x] 前端生产构建通过。

### 下一步
- 等待用户体验确认“一键增量更新”的三任务进度展示与执行结果是否符合预期。

### 已知问题 / 阻塞 / 待人工决策
- 无。
