# AlphaPredator 编码 Agent 运行准则

## 1. 项目简介

个人用A股助手。当前项目为起步阶段，不需要考虑多个。先做一个能跑的demo出来再看

## 2. 技术栈

后端：fastapi + sqlalchemy + pytest。python包管理用pip

数据库：sqlite

前端：react + typescript + vite

## 3. 第一版你要做什么

常见的股票行情展示软件。要求包含

- 日K数据展示。成交额、成交量、涨停跌停变色
- 均线、expma、macd/kdj等常见技术指标。相关指标参数不要在代码中写死，需要界面可以调整并在浏览器本地持久化；技术指标数值显示两位小数
- 股票搜索功能，6位数字代码+拼音简称
- 自选股和标签能力。手动给股票添加标签时自动加入自选；自选页面按标签自动分组，一只股票有多个标签时可以在多个分组中重复出现；左侧栏集中展示、搜索、新建、排序、重命名和删除标签；个股标签输入支持按前缀选择已有标签或直接新建

行情数据来源：接入thsdk作为行情数据来源。对于股票日K行情先采取在线获取，不本地保存。

考虑到后面有更换数据源的需求，需要做好数据屏蔽层

涨停数据：韭研公社涨停数据，具体接入方式参考master分支中相关实现。需要在个股页面用列表的方式列出相关的涨停时间和涨停原因



## 4. 第一版不做什么

- 不做mcp

## 5. 第一版成功标准

- 可以通过六位股票代码或拼音简称搜索并进入个股详情页
- 个股详情页可以展示最新行情和最近250个交易日的日K
- K线支持MA、EXPMA、MACD、KDJ指标切换
- 涨停、跌停及普通涨跌颜色显示正确
- 可以添加、删除自选股；自选页面按标签自动分组，无标签股票进入“未分类”；每个标签分组支持搜索股票并快速添加到该标签
- 可以给个股添加和删除多个自定义标签
- 左侧栏展示功能导航和标签列表；标签多时可搜索、独立滚动和拖动排序
- 自选股和标签在应用重启后仍然存在
- 行情数据通过统一Provider接口获取，业务层不直接依赖thsdk
- 个股页可以展示最近10条韭研公社涨停记录
- thsdk或韭研公社不可用时，页面显示明确错误，不使用伪造数据
- 后端核心测试通过，前端可以完成一次生产构建
